spring:
  main:
  banner-mode: off

logging:
  level:
    org:
      springframework:
        web: DEBUG
      hibernate: ERROR

employee:
  service:
    host: localhost

ivan:
  test:
    name: Ivan

clients:
  externalClient.url: http://localhost:${wiremock.server.port}/externalClient/